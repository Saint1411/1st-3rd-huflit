@model BookStoreManager.Models.THELOAI

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h2 style="color:red">Delete Categories</h2>

    <h3 style="text-align:center;">Are you sure you want to delete this?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ID)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ID)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.NameCate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.NameCate)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <a class="btn btn-secondary" href="@Url.Action("Index","Categories")"><i class="fa-solid fa-arrow-left"></i> Back</a> |
                <input type="submit" value="Delete" class="btn btn-danger" />
            </div>
        }
    </div>
</div>
